<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/review_item_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:paddingBottom="8dp"
    android:paddingTop="8dp"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        android:weightSum="10">

        <TextView
            style="@style/TextAppearance.MaterialComponents.Subtitle2"
            android:id="@+id/review_author_title"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:text="@string/ui_review_author_title"/>

        <TextView
            style="@style/TextAppearance.MaterialComponents.Subtitle2"
            android:id="@+id/review_author"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="9"
            android:gravity="start"
            tools:text="Jon English"/>
    </LinearLayout>

    <TextView
        style="@style/TextAppearance.MaterialComponents.Body2"
        android:id="@+id/review_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        tools:text="Toolbar has special handling for it's minimum height to declare the amount of
         space which is used for buttons (and actions). In the example below, we're setting the
          height to be 128dp (which is 56dp + 72dp as defined in the spec), but keeping the
           android:minHeight as the standard actionBarSize (which is usually 56dp)."/>
</LinearLayout>

